%h2.text-2xl.font-bold.mb-6 Edit #{resource_name.to_s.humanize}

= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: "space-y-4" }) do |f|
  = render "devise/shared/error_messages", resource: resource

  .field
    = f.label :email, class: "block text-sm font-medium text-gray-700"
    = f.email_field :email, autofocus: true, autocomplete: "email", class: "mt-1 block w-full border border-gray-300 px-2 py-1"

  - if devise_mapping.confirmable? && resource.pending_reconfirmation?
    %div
      Currently waiting confirmation for: #{resource.unconfirmed_email}

  .field
    = f.label :password, class: "block text-sm font-medium text-gray-700"
    %i (leave blank if you don't want to change it)
    = f.password_field :password, autocomplete: "new-password", class: "mt-1 block w-full border border-gray-300 px-2 py-1"
    - if @minimum_password_length
      %em
        (#{@minimum_password_length} characters minimum)

  .field
    = f.label :password_confirmation, class: "block text-sm font-medium text-gray-700"
    = f.password_field :password_confirmation, autocomplete: "new-password", class: "mt-1 block w-full border border-gray-300 px-2 py-1"

  .field
    = f.label :current_password, class: "block text-sm font-medium text-gray-700"
    %i (we need your current password to confirm your changes)
    = f.password_field :current_password, autocomplete: "current-password", class: "mt-1 block w-full border border-gray-300 px-2 py-1"

  .actions
    = f.submit "Update", class: "w-full py-2 px-4 bg-indigo-600 text-white font-semibold hover:bg-indigo-700 focus:outline-none"

%h3.text-lg.font-semibold.mt-6 Cancel my account
%div.mb-4
  Unhappy? 
  = button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete, class: "text-red-600 underline"

= link_to "Back", :back, class: "text-blue-600 underline"
